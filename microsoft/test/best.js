/* eslint-disable no-undef */
const assert = require('assert');

describe('best', () => {
  describe('instantiation', () => {
    it('does it rick roll?', () => {
      assert(true);
    });
  });
});
